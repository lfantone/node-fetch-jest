{
  "name": "node-starter",
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "test": "NODE_OPTIONS='--experimental-vm-modules --no-warnings' jest --env=node --colors --bail --detectOpenHandles --setupFilesAfterEnv=jest-extended/all --errorOnDeprecated --noStackTrace --forceExit --logHeapUsage src/"
  },
  "dependencies": {
    "node-fetch": "^3.2.0"
  },
  "devDependencies": {
    "jest": "^27.4.7",
    "jest-extended": "^1.2.0"
  },
  "engines": {
    "node": ">=16.0.0"
  }
}
